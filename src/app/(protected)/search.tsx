import React, { type FC } from 'react';
import { SearchScreen } from '../../modules';

/**
 * Search Modal Route Component
 * @returns {React.ReactElement} Search screen modal element.
 */
const Search: FC = (): React.ReactElement => {
  return <SearchScreen />;
};

export default Search;
