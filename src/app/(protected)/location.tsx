import React, { type FC } from 'react';
import { LocationScreen } from '../../modules';

/**
 * The Location Component
 * Provides a screen for setting user location.
 * @returns {React.ReactElement} The Location Screen.
 */
const Location: FC = (): React.ReactElement => {
  return <LocationScreen />;
};

export default Location;
